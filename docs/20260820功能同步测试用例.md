# 诵经助手功能同步测试说明

日期：2026-08-20

## 自动化验证

- `flutter test`：25 项全部通过（含 TTS 完成状态、AppToast Overlay、善书朗读页序与双页分组回归）。
- 数据库：覆盖全新 schema v2、v1 → v2 增量升级、旧数据和个人状态保留。
- 导入导出：覆盖旧格式/v2、Swift/秒/毫秒/ISO 日期、三种冲突策略、批处理隔离、导出往返、UTF-8/schema/Base64/文件及图片大小限制。
- 每日开示：覆盖顺序/随机同日稳定、跨日、负日期边界、刷新防重复、重启后收藏/完成/评论/排序保持。
- 十念法：覆盖内置节奏、自定义节奏持久化、引用替换、损坏配置回退、引磬期间停止、暂停继续不重复。
- 历史日期：覆盖秒、毫秒、数字字符串和 ISO 8601。

## 构建与静态检查

- Windows release 构建成功：`build/windows/x64/runner/Release/gongke.exe`。
- `flutter analyze --no-fatal-warnings --no-fatal-infos` 退出码 0，无 analyzer error；当前仓库报告 116 条 warning/info（未作为本轮功能阻断）。
- 新增及修改的 Dart 文件均已格式化，`git diff --check` 通过。

## 待人工验证

- Windows 核心流程：安装/启动、TTS、三种木鱼音色、文件选择、导入导出、复制/分享及页面退出资源释放。
- Android release 与真机核心流程；当前机器未配置 Android SDK。
- Windows/Android 扬声器、耳机和蓝牙音频。
- 使用脱敏的真实 v1 用户数据库比对升级前后数量与状态。
- AppToast：成功/警告/错误样式正确，顶部通知不遮挡主要操作，连续消息按队列展示，复制按钮可用。
- 善书听书：Windows 单页/双页均有入口；从当前页连续朗读并同步翻页；停止、手动翻页、缩略图跳转和退出页面后不再继续播放。
